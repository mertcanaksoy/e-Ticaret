@model IEnumerable<VTYS_Proje.Models.Adress>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Musteri.Aciklama)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AdresAdi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AdresAlani)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Musteri.Aciklama)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AdresAdi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AdresAlani)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AdresID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AdresID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AdresID })
        </td>
    </tr>
}

</table>
