@model IEnumerable<VTYS_Proje.Models.SatinAlma>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Personel.PersonelID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tedarikci.TedarikciAdi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SatinAlmaTarihi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Personel.PersonelID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tedarikci.TedarikciAdi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SatinAlmaTarihi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SatinAlmaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SatinAlmaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SatinAlmaID })
        </td>
    </tr>
}

</table>
