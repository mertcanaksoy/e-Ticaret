@model IEnumerable<VTYS_Proje.Models.SiparisDurum>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SiparisDurumAdi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SiparisDurumAdi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SiparisDurumID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SiparisDurumID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SiparisDurumID })
        </td>
    </tr>
}

</table>
